---
 - name: Create a login user
   hosts: web
   become: yes
  
   tasks:
    - name: Ensure group dbadmin exists
      group:
       name: dbadmin
       state: present
      
    - name: Crea usuario
      user:
       name: "{{ item.username }}"
       state: present
       groups: "{{ item.groups }}"
      loop:
       - { username: 'ops1', groups: 'dbadmin' }
       - { username: 'ops2', groups: 'dbadmin' }
       - { username: 'ops3', groups: 'webadmin' }
    
